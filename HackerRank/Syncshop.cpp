#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <vector>
#include <map>
using namespace std;
vector<int> typefish;
map<int,vector<int>> fish;

int main() {
    long n,m,k,i,tyno,ty,j;
    cin>>n>>m>>k;
    for(i=1;i<=n;i++){
        cin>>tyno;
        typefish.clear();
        for(j=0;j<ty;j++)
        {
            cin>>ty;
            typefish.push_back(ty);
        }
        fish[i].
        fish.insert(pair<int,vector<int>>(i,typefish))
    }

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    return 0;
}
