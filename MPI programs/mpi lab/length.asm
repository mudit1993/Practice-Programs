DATA SEGMENT
MSG DB "MANIDEEPA $"
DATA ENDS
CODE SEGMENT

ASSUME CS: CODE, DS: DATA
START:
	MOV SI,OFFSET MSG
	MOV AL,0

	L1:CMP[SI],"$"
	JE EXIT
	INC CL
	INC SI
	JMP L1

	EXIT:
		MOV AH,02H
		ADD CL,48
		MOV DL,CL
		INT 21H
CODE ENDS
END START